HackerSchoolExampleHack
=======================

ExampleHack

These programs can be compiled by typing the following into the command line:

g++ fizzbuzz.cpp -o fizzbuzz
g++ example.cpp -o numbergame

And to run it, type into the command line

./fizzbuzz
./numbergame

